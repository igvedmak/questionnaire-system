"""LLM integration — AI-powered template generation and response analysis.

Requires the 'llm' optional extra: pip install questionnaire[llm]

The API key is read from QST_LLM_API_KEY env var (or .env file).
"""
